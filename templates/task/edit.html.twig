{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-lg-6 card shadow-lg p-5 mb-5 border-0">
            {{ form_start(form, {'action' : path('task_edit', { 'id' : task.id })}) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-success rounded-pill">Modifier</button>
            {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
